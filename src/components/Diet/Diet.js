import React, { PureComponent, PropTypes } from 'react'

class Diet extends PureComponent {
  render () {
    return (
      <div>Diet</div>
    )
  }
}

Diet.propTypes = {}
Diet.defaultProps = {}

export default Diet
